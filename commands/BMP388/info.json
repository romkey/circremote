{
  "description": "BMP388 is a high-precision barometric pressure sensor that measures atmospheric pressure and temperature with ultra-low noise. It features high accuracy for altitude measurement applications and includes configurable oversampling, filtering, and power modes. The sensor uses I2C communication and is designed for applications requiring precise pressure and altitude measurements.",
  "warn_offline": false,
  "warn_unavailable": false,
  "variables": [
    {
      "name": "sda",
      "required": false,
      "description": "I2C SDA pin",
      "default": "board.SDA"
    },
    {
      "name": "scl",
      "required": false,
      "description": "I2C SCL pin",
      "default": "board.SCL"
    },
    {
      "name": "address",
      "required": false,
      "description": "I2C address",
      "default": "0x77"
    }
  ]
}
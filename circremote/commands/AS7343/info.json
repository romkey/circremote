{
  "description": "AS7343 is a 14-channel multi-spectral sensor from ams OSRAM that measures light from violet through near-infrared. Its auto-SMUX hardware cycles through up to 18 data channels per read, including visible clear channels and NIR, and it supports configurable gain, integration time, and flicker detection at 50/60 Hz. The sensor communicates via I2C.",
  "variables": [
    {
      "name": "sda",
      "required": false,
      "description": "I2C SDA pin",
      "default": "board.SDA"
    },
    {
      "name": "scl",
      "required": false,
      "description": "I2C SCL pin",
      "default": "board.SCL"
    },
    {
      "name": "address",
      "required": false,
      "description": "I2C address",
      "default": "0x39"
    }
  ],
  "tested": false,
  "warn_offline": false
}

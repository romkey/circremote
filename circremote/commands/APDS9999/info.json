{
  "description": "APDS9999 is a digital RGB, IR, and proximity sensor from Broadcom with an integrated IR LED. It reports red, green, blue, and infrared counts along with a calculated lux value and proximity, and supports configurable resolution, measurement rate, gain, LED current, and interrupt thresholds. It communicates via I2C.",
  "variables": [
    {
      "name": "sda",
      "required": false,
      "description": "I2C SDA pin",
      "default": "board.SDA"
    },
    {
      "name": "scl",
      "required": false,
      "description": "I2C SCL pin",
      "default": "board.SCL"
    },
    {
      "name": "address",
      "required": false,
      "description": "I2C address",
      "default": "0x52"
    }
  ],
  "tested": false,
  "warn_offline": false
}
